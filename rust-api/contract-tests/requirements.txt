# Contract-test dependencies (test runner side only).
# The Django server under test uses apps/api/requirements/*.txt.
celery>=5.3
httpx==0.28.1
psycopg[binary]==3.3.0
pika>=1.3
requests>=2.31
redis

pytest==9.0.3

# Rules (from PIDASHCONV-1, binding):
# - Never import Django or use its test client. HTTP via httpx, seeding via SQL.
# - Extend ``_harness/``, never fork it per domain.
